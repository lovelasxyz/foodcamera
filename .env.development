# Development defaults
# Local mode: do not call backend, use local probability logic
VITE_FORCE_LOCAL_SPIN=true
# Optional backend base (ignored when FORCE_LOCAL_SPIN=true)
VITE_SPIN_API_URL=http://localhost:3000/api
# Legendary rarity scale (0.001..0.2); lower = rarer
VITE_LEGENDARY_SCALE=0.05
